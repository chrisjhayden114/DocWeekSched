# Mobile app — OUT OF SCOPE

This Expo / React Native shell is **frozen**. Product strategy is **PWA-only** (see `PRODUCT_SPEC.md`).

- Do **not** extend features, screens, or API clients in this package.
- Do **not** include `apps/mobile` in CI build/test matrices.
- The web app (`apps/web`) is the mobile experience via PWA (Phase 4).

This directory is retained for historical reference only.
